# Core OpenAI
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4.1-mini
OPENAI_STORYBOARD_MODEL=gpt-4.1-mini
OPENAI_IMAGE_MODEL=gpt-image-2
OPENAI_VIDEO_MODEL=sora-2

# Azure AI Foundry / Azure OpenAI
AZURE_AI_FOUNDRY_ENDPOINT=
AZURE_AI_FOUNDRY_KEY=
AZURE_AI_FOUNDRY_MODEL=Phi-4
AZURE_AI_FOUNDRY_API_VERSION=
AZURE_AI_FOUNDRY_IMAGE_MODEL=gpt-image-2

AZURE_EXISTING_AIPROJECT_ENDPOINT=
AZURE_OPENAI_COMPLETIONS_KEY=
AZURE_OPENAI_COMPLETIONS_MODEL=

AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_KEY=
AZURE_OPENAI_API_VERSION=2025-04-01-preview
AZURE_OPENAI_TEXT_MODEL=
AZURE_OPENAI_STORYBOARD_MODEL=
AZURE_OPENAI_POLISH_MODEL=
AZURE_OPENAI_VISION_MODEL=
AZURE_OPENAI_IMAGE_DEPLOYMENT=gpt-image-2
AZURE_OPENAI_VIDEO_DEPLOYMENT=

AZURE_VIDEO_ENDPOINT=
AZURE_VIDEO_KEY=
AZURE_VIDEO_MODEL=sora-2

# Optional IQ briefing configuration
HYROGLYPHIS_IQ_PROVIDER=auto
AZURE_FOUNDRY_IQ_AGENT_NAME=
AZURE_FOUNDRY_IQ_AGENT_VERSION=
AZURE_OPENAI_IQ_MODEL=gpt-4.1-mini
# Optional explicit Foundry project responses endpoint for the IQ agent
# (e.g. https://<resource>.services.ai.azure.com/api/projects/<project>/openai/v1).
# If unset, derived from AZURE_OPENAI_COMPLETIONS_ENDPOINT or AZURE_AI_FOUNDRY_ENDPOINT.
AZURE_FOUNDRY_IQ_ENDPOINT=
# Auth mode for the Foundry IQ agent: auto (default) | aad | api-key.
# Agents whose tools use OBO (on-behalf-of) auth REQUIRE a Microsoft Entra ID
# token and cannot be called with an API key. 'auto' tries Entra ID first
# (via DefaultAzureCredential: az login / managed identity) then falls back to
# the API key. Use 'aad' to force Entra ID, 'api-key' to force the key.
AZURE_FOUNDRY_IQ_AUTH=auto
# Entra ID token scope for the agent call (default: https://ai.azure.com/.default).
AZURE_FOUNDRY_IQ_SCOPE=https://ai.azure.com/.default

# ARV Thumbnail Studio
# Where generated style profiles, sessions and exported thumbnails are stored.
ARV_THUMBNAIL_DATA_DIR=data/thumbnail-studio
# Optional default folder of reference thumbnails analyzed on the server.
ARV_THUMBNAIL_DEFAULT_REFERENCE_DIR=

# Optional ARV live auth
ARV_OPERATOR_TOKEN=
